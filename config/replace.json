{
    "dev": {
        "src": [
            "dev/*.html"
        ],
        "dest": "dev/",
        "replacements": [
            {
                "from": "{{%release.version%}}",
                "to": "<%= pkg.version %>"
            }
        ]
    }
}
